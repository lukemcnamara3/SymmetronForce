# SymmetronForce
Code which builds the classical field profile in python, and then uses a greens function method in mathematica to compute the solutions to the quantum symmetron field. Note that the files 'qfield.wl' and 'quantum_field.nb' are the same file, however git has a problem with reading the files in this format. 
